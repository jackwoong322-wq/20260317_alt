## 무엇을 발견
- `renderBoxMarks` 내 중첩 함수 `addForecastGuide`에서 TypeScript 타입 내로잉이 전파되지 않아 `overlay` 변수가 null일 수 있는 경로가 잔존
- 외부 스코프에서 null 체크를 통과했더라도, 중첩 함수(클로저) 내부에서는 TS 컴파일러가 narrowing을 전파하지 않으므로 타입 오류 및 런타임 위험 존재

## 무엇을 수정
- `01_pairUSDT/templates/chart-render-overlays.ts`: `addForecastGuide` 함수 첫 줄에 `if (!overlay) return;` 명시적 가드 추가 (주석 포함)
- `03_frontend/public/legacy/dist/chart-render-overlays.js`: 빌드된 JS 파일에 동일 가드 반영

## 왜 그렇게 판단
- TypeScript는 클로저 내부에서 외부 스코프의 타입 내로잉을 재평가하지 않음
- 명시적 가드를 추가하는 것이 가장 안전하고 의도를 명확히 드러내는 방식
- 동작 변경 없이 방어 코드만 추가하므로 사이드이펙트 없음

## 테스트
- 별도 단위 테스트 없음 (방어 코드 추가로 기존 동작 변경 없음)
- 린트·타입체크: 이상 없음

## PR
- #97 / 사양서: 없음 (버그 수정만)
