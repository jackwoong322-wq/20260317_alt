# 이슈 #101 보고서

## 무엇을 발견
- 이전 구현에서 COIN/CYCLES/SHOW를 size>1 리스트박스로 만든 것이 이슈의 원인
- 이슈 AC 요구사항: "COIN과 동일한 드롭다운 스타일로 렌더링"
- 사용자 선택 방향: B (클릭 시 체크박스 패널이 펼쳐지는 커스텀 드롭다운)

## 무엇을 수정
- chart-shell-v2.html: `<select>` 3개 → `.custom-dropdown` 구조 (trigger + panel) 로 교체
- chart-ui.js: 전체 재작성 — buildCoinList(), buildCycleToggles(), buildShowDropdown(), syncShowDropdown(), initDropdownToggle(), updateTriggerLabel()
- chart-v2.css: .custom-dropdown, .dropdown-trigger, .dropdown-panel, .dropdown-item 등 스타일 추가

## 왜 그렇게 판단
- `<select multiple>`은 size>1일 때 리스트박스, 드롭다운 형태가 아님
- 커스텀 드롭다운만이 "클릭 시 펼쳐지는 드롭다운 + 멀티선택" 두 조건을 동시에 만족
- 외부 클릭 시 닫힘, 최소 1개 선택 유지 등 UX 보호 로직 포함

## 테스트
- 26개 통과 / 03_frontend Vitest

## PR
- Closes #101 / 사양서: 추가 없음 (UI 스타일 변경)
